---
import SiteShell from '@/layouts/SiteShell.astro';
import Container from '@/components/layout/Container.astro';
import Breadcrumbs from '@/components/layout/Breadcrumbs.astro';
import { getDecisionProfileBySlug } from '@/lib/data/entities';
import { LlmFrameworkDecision } from '@/components/decisions/LlmFrameworkDecision';

const profile = getDecisionProfileBySlug('llm-framework');
if (!profile) {
  throw new Error('Missing decision profile: llm-framework');
}
---

<SiteShell title={`${profile.name} — AI Atlas`} description={profile.summary}>
  <Container class="flex flex-col gap-6 py-10">
    <Breadcrumbs
      items={[
        { label: 'Home', href: '/' },
        { label: 'Decisions', href: '/decisions' },
        { label: profile.name },
      ]}
    />
    <LlmFrameworkDecision client:load profile={profile} />
  </Container>
</SiteShell>
